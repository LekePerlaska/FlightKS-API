POSTGRES_PASSWORD=<POSTGRES_PASSWORD>
POSTGRES_USER=<POSTGRES_USER>

KC_DB_USER=<KC_DB_USER>
KC_DB_PASSWORD=<KC_DB_PASSWORD>
KC_ADMIN_USER=<KC_ADMIN_USER>
KC_ADMIN_PASSWORD=<KC_ADMIN_PASSWORD>

KEYCLOAK_ADMIN_CLIENT_ID=flightks-admin-client
KEYCLOAK_ADMIN_CLIENT_SECRET=<KEYCLOAK_ADMIN_CLIENT_SECRET>
KEYCLOAK_FRONTEND_CLIENT_ID=flightks-client

REDIS_PASSWORD=<REDIS_PASSWORD>


GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=<GRAFANA_ADMIN_PASSWORD>

# Nginx reverse proxy runs under the production profile
# (docker compose --profile production up). It uses path-based routing on a
# single host (config in nginx/conf.d/default.conf) and needs no env vars yet.
# Add TLS cert/domain settings here when HTTPS is introduced.
